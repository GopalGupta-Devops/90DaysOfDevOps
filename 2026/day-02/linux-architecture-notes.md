# Today is Day 2 

In Linux Kernal is a heart of linux operating system, It connect to hardware.

User space is where applications and users operate like bash shell, application and libraries.

Init is a first process start by the Kernal, it's also know as a systemd, there is manage system backgroud services and handles logging. 


A process is created when the operating system loads a program into memory and assigns it resources like CPU, memory, and I/O. Each process gets a unique identifier for tracking.
